<!--<p id="notice"><%#= notice %></p>-->

<p align="right" class="navbar-text pull-left">
  <% if student_signed_in? %>
    Logged in as <strong> <%= current_student.email %></strong>.
    <%= link_to "Logout", destroy_student_session_path, method: :delete, :class => 'navbar-link'  %>
    <%= link_to 'Edit profile', edit_student_registration_path,  :class => 'navbar-link' %>
  <%end %>
  <% if student_signed_in? %>
    <h1><%= current_student.name %>'s Student Dashboard</h1>
    <h3><%= link_to 'View libraries', getstudentlibraries_path(:university_id => current_student.university_id) %></h3>
    <h3><%= link_to 'Manage Books', getstudentbooks_path(:university_id => current_student.university_id)%></h3>
    <h3><%= link_to 'Manage Book Hold Requests', showrequests_path %></h3>
    <h3><%= link_to 'View Profile', showstudentprofile_path%></h3> 
    <h3><%= link_to 'Search Books', search_path%></h3> 
    <h3><%= link_to 'View Bookmarks', showbookmarks_path %></h3> 
    <h3><%= link_to 'View Overdue fine', studentoverduefine_path(:student_id => current_student.id) %></h3>
  <%end %>
  

  

